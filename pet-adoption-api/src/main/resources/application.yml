server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petadoption
    username: root
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
